

# Aumentar tamaño de horas en lista lateral del TV Dashboard

## Cambios en `src/components/monitoring/tv/TVServicesList.tsx`

1. **Separador de bloque horario** (linea 76): Cambiar `text-sm` (14px) a `text-lg` (18px)
2. **Hora en cada fila de servicio** (linea 98): Cambiar `text-base` (16px) a `text-xl` (20px)

Dos cambios puntuales de clase CSS, sin modificar estructura ni logica.

